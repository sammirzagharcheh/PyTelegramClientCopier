# UAT overlay — staging-like ports; API still reachable for integration testing.
services:
  mongodb:
    ports:
      - "27020:27017"

  backend:
    env_file:
      - ../env/docker.env.uat
    ports:
      - "8082:8000"
      - "8003:8000"
